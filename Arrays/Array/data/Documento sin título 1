/* Cómo havcer array multidimensional.

Para recorrer arrays multidimensionals:

let dades;

for(let i= 0; ....

console.log(dades[i].name);

arrayPokemons.push(dades[i].name

Para hacer un array para cada uno de los poquemons.

let pokem=[];

pokemon.push(dades[i].name)
pokemon.push(dades[i].img);


arrayPokemns.push(pokemon)

de esta manera tenemos un array multidimensional sería:

 */
